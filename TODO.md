# TODO
- [ ] Inspect the current View modal HTML + JS inside `work-order.php` (completed via read, but verify exact block to replace).
- [ ] Replace `work-order.php` View modal markup to match `client-view.php` (same ids/classes/sections).
- [ ] Update `work-order.php` JS to populate the `vw_*` ids and reuse the client-view rendering logic for stepper, notes, parts, and payment summary.
- [ ] If returned JSON fields differ, map them appropriately inside `work-order.php`.
- [ ] Basic verification checklist (open page, click View, confirm rendering and show/hide behavior).

