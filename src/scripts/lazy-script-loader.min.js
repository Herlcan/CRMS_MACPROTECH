(function () { if (window.MacproScriptLoader) { return; } const loadedScripts = {}; function load(src) { if (!src) { return Promise.reject(new Error('Missing script source')); } if (loadedScripts[src]) { return loadedScripts[src]; } loadedScripts[src] = new Promise(function (resolve, reject) { const existing = document.querySelector('script[src="' + src.replace(/"/g, '\\"') + '"]'); if (existing && existing.dataset.loaded === 'true') { resolve(existing); return; } const script = existing || document.createElement('script'); script.src = src; script.defer = true; script.addEventListener('load', function () { script.dataset.loaded = 'true'; resolve(script); }, { once: true }); script.addEventListener('error', function () { delete loadedScripts[src]; reject(new Error('Unable to load ' + src)); }, { once: true }); if (!existing) { document.head.appendChild(script); } }); return loadedScripts[src]; } function runAfterLoad(callback, timeout) { function schedule() { if ('requestIdleCallback' in window) { window.requestIdleCallback(callback, { timeout: timeout || 1500 }); return; } window.setTimeout(callback, 350); } if (document.readyState === 'complete') { schedule(); } else { window.addEventListener('load', schedule, { once: true }); } } window.MacproScriptLoader = { load: load, runAfterLoad: runAfterLoad }; })();
